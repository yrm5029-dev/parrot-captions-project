__pycache__/
*.pyc
.DS_Store
data/
venv/
.env
*.jpg
*.png
# Keep results but maybe ignore large checkpoints if desired, 
# for now we strictly strictly keep what's needed.
# logic: we want results/adapter/adapter_final.pth included if it's small enough (<100MB)
# It is 1MB so it is fine.
